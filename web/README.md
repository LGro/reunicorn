# Web

To build the web version, place the output of `veilid/veilid-wasm/wasm_build_dart.sh`, specifically the `veilid/target/wasm32-unknown-unknown/debug/pkg/` directory as `wasm/` in this directory.
